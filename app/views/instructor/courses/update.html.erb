<br />
<div class="booyah-box col-xs-10 col-xs-offset-1">
  <h1>Update a lesson</h1>
  <%= simple_form_for @lesson, url: instructor_section_lessons_path(current_user) do |f| %>
    <%= f.input :title %>
    <%= f.input :subtitle %>
    <%= f.input :video %>
    <br />
        
    <%= f.submit "Update", class: 'btn btn-primary' %>
    <br />
    <br />
  <% end %> 
</div>